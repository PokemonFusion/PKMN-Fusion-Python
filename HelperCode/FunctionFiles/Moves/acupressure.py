def onHit (target):
	"""function (target) {
			var stats = [];
			var stat;
			for (stat in target.boosts) {
				if (target.boosts[stat] < 6) {
					stats.push(stat);
				}
			}
			if (stats.length) {
				var randomStat = this.sample(stats);
				var boost = {};
				boost[randomStat] = 2;
				this.boost(boost);
			}
			else {
				return false;
			}
		}
	""" 
	pass