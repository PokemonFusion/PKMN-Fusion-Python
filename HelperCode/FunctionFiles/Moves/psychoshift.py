def onTryHit (target, source, move):
	"""function (target, source, move) {
			if (!source.status)
				return false;
			move.status = source.status;
		}
	""" 
	pass

def onHit (pokemon):
	"""function (pokemon) {
				pokemon.cureStatus();
			}
	""" 
	pass