def onBoost (boost, target, source, effect):
	"""function (boost, target, source, effect) {
			if (effect && effect.id === 'zpower')
				return;
			var i;
			for (i in boost) {
				boost[i] *= -1;
			}
		}
	""" 
	pass